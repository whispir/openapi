{
  "name": "whispir-node-test",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "env-cmd": "^10.1.0"
  },
  "scripts": {
    "dev": "yarn build --verbose --watch",
    "test": "env-cmd node test.js"
  }
}
